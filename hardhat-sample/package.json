{
  "name": "hardhat-sample",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:ganache": "hardhat --config hardhat.config.ts ignition deploy --deployment-id rhie-ganache-chain --network ganache --reset",
    "deploy:polygon": "hardhat compile & hardhat --config hardhat.config.ts ignition deploy --deployment-id rhie-polygon-chain --network polygon",
    "hardhat:test": "hardhat test"
 },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.7"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0"
  }
}
